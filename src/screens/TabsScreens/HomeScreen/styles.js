import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  rootContainer: {
    flex: 1,
    backgroundColor: "#FBFBFB",
    padding: 16,
  },
  container: {
    flex: 1,
  },
  margin: {
    marginBottom: 48,
  },
});
